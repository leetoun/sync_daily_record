配置TPLINK路由器，为一个自定义局域网(192.168.22.xxx)

首先，reset路由器；

然后，设置登录密码，跳过WAN口的设置；
进入LAN口的ip地址设置，选择为“手动”修改，如修改为 192.168.22.11(这个是路由器的地址应该，因为设置为192.168.22.1是行不通的)，继续修改“子网掩码”和 “DNS”;
设置"DHCP", 或者关闭DHCP，或者设置到相应router的ip地址范围内。

最后，进行主机ping的测试。